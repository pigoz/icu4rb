# frozen_string_literal: true

RSpec.describe Icu4rb do
  it "has a version number" do
    expect(Icu4rb::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
